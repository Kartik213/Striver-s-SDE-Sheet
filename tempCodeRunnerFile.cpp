

        //     if (missing != -1 && twice != -1)
        //         break;
        // }

        // Hash map
        int hash[n + 1] = {0}; // hash array
        for (int i = 0; i < n; i++)
        {